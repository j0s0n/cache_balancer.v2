server {
        listen 80;
        listen [::]:80;
        server_name esteban.plexus.local;

        location / {
                root /usr/share/nginx/html;
                index index.html;
        }
}
